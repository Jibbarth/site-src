{% set url = path('article_detail', {type: article.type, article: article.id}) %}
{% if article.type == constant('App\\Constant\\ArticleType::EXTERNAL') %}
    {% set url = article.url %}
{% endif %}
<div class="border-b p-4">
    <a href="{{ url }}"
       class="text-lg text-foreground font-semibold hover:underline"
        {% if article.type == constant('App\\Constant\\ArticleType::EXTERNAL') %}
            target="_blank" rel="noopener,noreferrer"
        {% endif %}
    >
        {% if article.type == constant('App\\Constant\\ArticleType::EXTERNAL') %}
            {{ ux_icon('fa-solid:external-link-alt', {'class': 'icon-inline'}) }}
        {% endif %}
        {{ article.title }}
    </a>
    <div class="flex justify-between">
        <div>
            <span class="inline-block
                text-xs mr-2 my-1 uppercase tracking-wider border px-2
                text-muted-foreground border-border hover:bg-muted hover:text-foreground cursor-default">
                {{ article.type }}
            </span>
        </div>
        <div class="text-right text-sm text-muted-foreground">
            {{ ux_icon('fa-solid:calendar-day', {'class': 'icon-inline'}) }}
            {{ article.date|format_date() }}
        </div>
    </div>
</div>
